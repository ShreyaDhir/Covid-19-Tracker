import React from 'react'

function Table({ countries }) {
  return (
    <div className="table">
      {countries.map(country => {
        tr>td*2
        
        
      })}
    </div>
  )
}

export default Table
